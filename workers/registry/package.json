{
	"name": "registry-worker",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "bun check:biome && bun check:types",
		"check:biome": "biome check .",
		"check:types": "bun cf-typegen && tsc --noEmit",
		"cf-typegen": "wrangler types",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"test": "bun test"
	},
	"dependencies": {
		"hono": "^4.7.0",
		"ocx-schemas": "workspace:*",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"bun-types": "^1.3.5",
		"typescript": "^5.8.3",
		"wrangler": "^4.16.0"
	}
}
